{
  "name": "automate.fedramp.gov",
  "version": "1.0.0",
  "description": "automate.fedramp.gov website",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "pages": "git submodule update --init && hugo -d _site"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "hugo-extended": "^0.125.6"
  }
}
